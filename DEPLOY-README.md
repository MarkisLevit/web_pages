See [deploy/README.md](deploy/README.md) — the deployment guide lives next to the scripts.

Note: the nginx + certbot instructions that used to be in this file were wrong for
this server. The VPS runs Caddy and already serves markislevit.online; installing
nginx would take that site down.
